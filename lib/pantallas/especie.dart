import 'package:flutter/material.dart';

class Especie extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('HOLA MUNDITO especie'),
    );
  }
}
