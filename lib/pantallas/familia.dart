import 'package:flutter/material.dart';

class Familia extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('HOLA MUNDITO Familia'),
    );
  }
}
